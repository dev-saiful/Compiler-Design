#include<bits/stdc++.h>
using namespace std;
int main()
{
    char pattern[100];
    char str[100];
    int len1 = strlen(str), searchIndex, i, j;
    int len2 = strlen(pattern);
    cout << "Enter String: ";
     gets(str);
    cout << "Enter Pattern: ";
    gets(pattern);
    cout << pattern<<endl;
}
